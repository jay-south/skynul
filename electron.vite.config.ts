import { resolve } from 'path'
import { defineConfig } from 'electron-vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  main: {
    build: {
      rollupOptions: {
        input: {
          // Bootstrap first to patch module resolution before loading app code.
          index: resolve(__dirname, 'src/main/bootstrap.ts'),
          // Actual app entry (previous default).
          app: resolve(__dirname, 'src/main/index.ts')
        }
      }
    }
  },
  preload: {},
  renderer: {
    resolve: {
      alias: {
        '@renderer': resolve('src/renderer/src')
      }
    },
    plugins: [react()]
  }
})
